# Generated by Wrangler on Wed Apr 29 2026 11:15:40 GMT+0700 (Indochina Time)
# CANONICAL project name = the Pages project that OWNS the custom domains
# (nguyenlananh.com / www / admin) + D1 binding + production secrets, inside the
# official account 62d57eaa548617aeecac766e5a1cb98e (Anhhatam@gmail.com).
# Verified 2026-06-03: live domain serves THIS project. Deploying to the old
# empty "nguyenlananh-com" project is what caused the stale-build incident.
name = "nguyenlananh-com-63s"
# Repo root holds BOTH static assets (index.html, assets/, en/ …) and the
# functions/ directory. An empty output dir made the Git-connected build skip
# Functions compilation (no _routes.json, /api/* served as static 404/405).
# "." = project root; Cloudflare special-cases the top-level functions/ dir and
# compiles it into Pages Functions instead of serving it as static assets.
pages_build_output_dir = "."
compatibility_date = "2026-02-09"

# OFFICIAL production account that holds the nguyenlananh.com DNS zone.
# Pinning account_id forces every deploy to land here, preventing the
# cross-account mismatch that caused Cloudflare error 1014 (CNAME Cross-User
# Banned). Account: Anhhatam@gmail.com. Do NOT change without updating DNS.
account_id = "62d57eaa548617aeecac766e5a1cb98e"

[env.production]

[[env.production.d1_databases]]
binding = "PAYMENTS_DB"
database_name = "nguyenlananh-payments-prod"
database_id = "2f3a3331-546b-44f1-9992-57d18705afd5"

# Preview env KHÔNG dùng D1 cho đến khi có preview DB riêng.
# Tạo preview DB: wrangler d1 create nguyenlananh-payments-preview
# Sau đó uncomment khối dưới và cập nhật database_id.
# [env.preview]
#
# [[env.preview.d1_databases]]
# binding = "PAYMENTS_DB"
# database_name = "nguyenlananh-payments-preview"
# database_id = "<preview-database-id>"
