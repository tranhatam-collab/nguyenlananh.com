# UI Verify Report (Static Analysis)

**Date:** 2026-05-30
**Method:** Static grep/curl (no browser runtime available)
**Scope:** `index.html` (VI), `en/index.html` (EN)

---

## Checks

| Element | VI (`index.html`) | EN (`en/index.html`) | Notes |
|---------|-------------------|----------------------|-------|
| Hamburger button (`#hamburger`) | 1 | 1 | Present |
| Drawer (`#drawer`) | 1 | 1 | Present |
| `site.js` loaded | 1 | 1 | Present (after content-registry.js) |
| `cta-modules.js` loaded | dynamic via `site.js` | dynamic via `site.js` | Loaded by `loadScript("/assets/cta-modules.js")` in site.js |
| `content-registry.js` | 1 | 1 | Present (before site.js) |
| `i18n-config.js` | 1 | 1 | Present |
| `lang-routing.js` | 1 | 1 | Present |

## Drawer Logic

Inline script in both pages includes:
- `openDrawer()` / `shutDrawer()` functions
- Hamburger click toggles `aria-expanded`
- `Escape` key closes drawer
- Click outside drawer closes it
- `data-close` attribute on links auto-closes drawer

## CTA Panel

`cta-modules.js` is loaded dynamically by `site.js`. Content is locale-aware and self-filters (does not link to current page).

## Conclusion

**Status:** ✅ PASS (static verification)

All expected elements are present in markup. Drawer logic is inline in both homepages. CTA panel loads via dynamic script injection.

**Note:** Runtime verification (toggle animation, console errors, responsive layout) requires human browser test.
