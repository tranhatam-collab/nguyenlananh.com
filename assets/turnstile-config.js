// Turnstile site key configuration
// This file is auto-included before turnstile.js on pages that need bot protection.
// Cloudflare test key (always passes) — replace with real site key from dash.cloudflare.com → Turnstile
// Real site key starts with 0x4... and is public (safe to expose in client-side code).
window.TURNSTILE_SITE_KEY = window.TURNSTILE_SITE_KEY || "1x00000000000000000000AA";
